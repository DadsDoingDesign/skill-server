{
  "name": "skill-server",
  "version": "0.1.0",
  "description": "A shared MCP server for hosting and serving Claude/agent skills. Includes a web UI for managing skills and an HTTP-based MCP endpoint that any agent can connect to from anywhere.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "@vercel/blob": "^2.0.0",
    "adm-zip": "^0.5.16",
    "express": "^4.21.2",
    "gray-matter": "^4.0.3",
    "multer": "^2.0.1",
    "zod": "^3.23.8"
  }
}
